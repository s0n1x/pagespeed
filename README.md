# Page Speed API checker 

> pip install -r requirements.txt

По умолчанию создается коллекция `pagespeed_collectio` в базе `pagespeed` и пользователь:пароль `mongodb:mongodb`

> docker-compose up -d --build
